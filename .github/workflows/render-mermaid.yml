name: Render Mermaid Diagrams

on:
  push:
    branches: [ main, master ]
    paths:
      - 'diagram/**'
      - '.github/workflows/render-mermaid.yml'

jobs:
  render:
    runs-on: ubuntu-latest

    env:
      GIT_COMMITTER_NAME: github-actions[bot]
      GIT_COMMITTER_EMAIL: github-actions[bot]@users.noreply.github.com

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install mermaid-cli & dos2unix
        run: |
          npm install -g @mermaid-js/mermaid-cli
          sudo apt-get update -y
          sudo apt-get install -y dos2unix

      - name: Show raw .mmd file (first 200 chars & lines) - debug
        run: |
          echo "==== begin: diagram/architecture.mmd (head) ===="
          sed -n '1,40p' diagram/architecture.mmd || true
          echo "==== end: diagram/architecture.mmd (head) ===="
          echo "==== show as hex bytes (first 64 bytes) - detect BOM ===="
          head -c 64 diagram/architecture.mmd | xxd -g 1 -u || true

      - name: Normalize line endings and remove BOM
        run: |
          dos2unix diagram/architecture.mmd || true
          # remove utf-8 BOM if present
          tail -c +4 diagram/architecture.mmd > /tmp/arch.tmp || cp diagram/architecture.mmd /tmp/arch.tmp
          mv /tmp/arch.tmp diagram/architecture.mmd || true

      - name: Create assets folder
        run: mkdir -p assets

      - name: Create puppeteer config to disable sandbox (CI workaround)
        run: |
          cat > puppeteer-config.json <<'JSON'
          {
            "args": ["--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage"]
          }
          JSON

      - name: Render architecture.mmd -> assets/architecture.svg
        run: |
          mmdc -i diagram/architecture.mmd -o assets/architecture.svg -b transparent --width 1200 -p puppeteer-config.json

      - name: List generated asset
        run: ls -l assets || true

      - name: Commit generated SVG
        run: |
          git config user.name "$GIT_COMMITTER_NAME"
          git config user.email "$GIT_COMMITTER_EMAIL"
          git add assets/architecture.svg puppeteer-config.json || true
          git commit -m "chore: render mermaid -> assets/architecture.svg" || echo "no changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
